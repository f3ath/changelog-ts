{
  "name": "changelog-ts",
  "description": "Changelog document model, parser, and printer in TypeScript",
  "author": "Alexey Karpetov",
  "repository": "https://github.com/f3ath/changelog-ts",
  "license": "MIT",
  "version": "0.0.3",
  "main": "dist/index",
  "typings": "dist/index",
  "dependencies": {
    "marked-ts": "^1.0.0-beta.4",
    "semver": "^5.5.1"
  },
  "devDependencies": {
    "typescript": "^3.0.3",
    "ts-node": "^7.0.1",
    "@types/node": "^10.9.4",
    "@types/jasmine": "^2.8.8",
    "jasmine-ts": "^0.2.1"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "jasmine-ts test/**/*.spec.ts",
    "prepublish": "npm run build"
  },
  "files": [
    "dist"
  ]
}
